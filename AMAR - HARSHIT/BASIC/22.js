// For loop 

// print 0 to 9

for (let i = 0; i <= 9; i++) {
    console.log(i);
}

// console.log("value of i is ",i); // error - i is not defined.
// Because 'let i' is blocked scoped . Outside the block 'i' is in-accesible. In case of 'var i' we can access i
// outside the block.